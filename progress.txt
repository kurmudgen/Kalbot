KALBOT PROJECT STATUS — Last updated 2026-03-29 ~11:00 PM
================================================================

WHAT THIS IS:
Multi-strategy trading bot operating across 3 platforms simultaneously:
- Kalshi (prediction markets) — 9 strategies
- Alpaca (stocks + crypto paper trading) — blue chip + penny + crypto
- Coinbase (250+ crypto, connected but unfunded)

Uses a 3-model LLM ensemble (Perplexity + Claude + DeepSeek) for analysis,
with a local qwen2.5:7b model as pre-filter. Dynamic strategy rebalancing,
regime detection, and multiple intelligence feeds.

================================================================
PLATFORMS & API KEYS (.env, gitignored)
================================================================

Kalshi:
  KALSHI_API_KEY: 8680823e... (RSA-PSS signed requests)
  KALSHI_PRIVATE_KEY: kalshi_private_key.pem
  Balance: $50 real (PAPER_TRADE=true, no real trades)
  Auth: RSA key signing per request (pykalshi SDK)

Alpaca:
  ALPACA_API_KEY: PKZOEPY4...
  ALPACA_SECRET_KEY: AxcKxz...
  Balance: $99,975 paper ($100K start, -$25 from cleared positions)
  Mode: Paper trading (ALPACA_PAPER=true)
  Supports: stocks + 73 crypto pairs

Coinbase:
  COINBASE_API_KEY: organizations/ac597d77.../apiKeys/0226a819...
  COINBASE_PRIVATE_KEY: EC private key
  Balance: $0 (connected, ready when funded)
  Permissions: View + Trade only (no withdrawals)

LLM APIs:
  PERPLEXITY_API_KEY: pplx-pps... (Sonar Pro, web search)
  ANTHROPIC_API_KEY: sk-ant-api03-X-SP6... (Claude Sonnet 4)
  DEEPSEEK_API_KEY: sk-4387... (DeepSeek Chat)

Telegram:
  TELEGRAM_BOT_TOKEN: 8705795640:AAHz...
  TELEGRAM_CHAT_ID: 8063510080
  Bot: @kalbot_alerts_bot

================================================================
INFRASTRUCTURE
================================================================

Location: S:\kalbot (was ~/kalbot, moved for more space)
GitHub: https://github.com/kurmudgen/Kalbot (public)
Dashboard: https://kalbot-dashboard.vercel.app
Vercel: disconnected from GitHub auto-deploy (manual only)
  - Status.json pushed to GitHub every 5 min via git
  - Dashboard fetches from raw.githubusercontent.com client-side
  - Deploy manually: cd S:\kalbot\dashboard && npx vercel --prod

Python: 3.14.0, venv at S:\kalbot\.venv
Ollama: v0.18.2, qwen2.5:7b for local filter (~4.7GB VRAM)
GPU: NVIDIA 16GB VRAM

Auto-start: S:\kalbot\start_all.bat (Windows Task Scheduler on login)
Kill switch: create S:\kalbot\STOP file to halt all trading
Watchdog: bot/watchdog.py auto-restarts crashed processes

================================================================
HOW THE KALSHI BOT WORKS (bot/dual_strategy.py)
================================================================

Every 5 minutes, runs this pipeline:

1. KILL SWITCH CHECK — S:\kalbot\STOP file exists? Skip cycle.

2. REGIME DETECTION (bot/regime_detector.py)
   - Downloads S&P 500 prices via yfinance
   - Classifies BULL/BEAR/VOLATILE based on 20-day trend + volatility
   - Currently: BEAR (-5.7% trend, 12% vol)
   - Adjusts Kelly: bull=1.3x, bear=0.6x, volatile=0.5x

3. SEC EDGAR MONITOR (bot/edgar_monitor.py)
   - Fetches 8-K filings, NLP classifies bullish/bearish
   - Telegram alerts for material events

4. RESOLUTION TRACKER (bot/resolution_tracker.py)
   - Checks if traded markets resolved → actual P&L
   - Feeds outcomes to trade memory + adaptive weights + Telegram

5. CROSS-MARKET CORRELATION (bot/cross_market.py)
   - Flags when CPI/Fed/GDP markets diverge unexpectedly

6. EARLY EXIT (bot/early_exit.py)
   - Take profit at 60%, stop loss at 30% (polling-based)

7. POLYMARKET SIGNAL — Crawls PM for sharp moves, signals only

8. MARKET MAKER — Spread orders on thin weather markets

9. DATA SNIPER — Pre-researches upcoming CPI/Fed/jobs releases

10. QUANTITATIVE STRATEGIES (6 modules):
    SP500: VIX-implied brackets with VRP haircut
    BITCOIN: Jump-diffusion Monte Carlo (10K paths)
    ECON: Cleveland Fed + GDPNow + FedWatch (very conservative, >12% edge required)
    TREASURY: Yield change distribution
    GAS: EIA weekly + crude correlation
    FOREX: Implied vol bracket pricing

11. MARKET SCANNER — Raw Kalshi REST API by series ticker
    ~573 target markets, classifies by prefix, rejects sports bundles

12. LOCAL FILTER — qwen2.5:7b via Ollama
    Pre-filters: skip no-volume, resolved, recently scored
    Caps at 200/cycle, returns cached passes too

13. ENSEMBLE ANALYST — 3-model pipeline:
    Perplexity (researcher, web search) → Claude + DeepSeek (independent evaluators)
    Consensus: 2/3 majority + confidence floor >0.65
    Calibration: Platt scaling → edge dampening → spread penalty
    Adaptive weights: inverse-Brier per model per category

14. EXECUTOR — Safety gauntlet before every trade:
    Kill switch → bracket dedup → confidence floor → orderbook depth →
    seasonal adjustment → historical bias → regime Kelly → correlation guard →
    category thresholds → spend caps → paper/live mode
    Position sizing: Quarter-Kelly * regime_multiplier

15. STRATEGY REBALANCER — Shifts budget toward winners
    Default: WEATHER 30%, SP500 20%, BTC 12%, SNIPER 8%, ECON 8%,
    TREASURY 8%, EMERGING 6%, GAS 5%, FOREX 5%

16. AUTO-SCALER — $50→$200→$500→$1000 as win rate proves out

================================================================
HOW THE STOCK BOT WORKS (stock_bot/run_stock_bot.py)
================================================================

Every 5 minutes:

1. Liquidation cascade check — blocks crypto if funding rates extreme
2. Crypto strategies — BTC/ETH/SOL + meme coins (24/7)
3. Reddit velocity — detects accelerating mention counts
4. Macro lockout — CPI/Fed/jobs days → only Kalshi trades
5. Market hours check — stocks only 10:00-16:00 ET
6. Blue chip strategy — 30+ stocks, 3-model unanimous, $500 max, momentum filter
7. Penny scanner — 5 signal types with multi-signal boost
8. Market intelligence — Fear & Greed + Congressional trades injected

================================================================
KEY SAFETY FIXES FROM EXTERNAL REVIEW
================================================================

1. Bracket deduplication: one trade per underlying event
2. Confidence floor: ensemble avg >0.65 required
3. Macro lockout: CPI/Fed/jobs → only Kalshi trades
4. Position sizing: $500 max per stock (was $114K bug)
5. Duplicate check: won't buy same symbol twice
6. No-first-30-minutes: skips noisy market open

================================================================
TRAINING RESULTS
================================================================

13,743 markets trained, 4,582 validated:
  Train: 83.5% WR, 0.1398 Brier
  Val: 83.0% WR, 0.1426 Brier (0.5% gap = no overfitting)
  Weather: 89.4% WR (best category)

================================================================
EXPANSION — 2026-03-25
================================================================

New data feed modules (S:\kalbot\data\):
  - congressional_trades_feed.py — House/Senate stock filings
  - jobless_claims_feed.py — DOL initial/continuing claims via FRED
  - eia_petroleum_feed.py — EIA gas prices + crude/gasoline inventory
  - fed_funds_feed.py — Fed funds rate + yield curve via FRED
  - treasury_auction_feed.py — Treasury Direct auctions + FRED yields
  - box_office_feed.py — Weekend box office tracking

Scanner expansion (bot/market_scanner.py):
  - New categories: congressional, energy, entertainment
  - New series tickers: KXCONG, KXSTOCK, KXOIL, KXWTI, KXMOVIE, KXBOX
  - KXEURUSD/KXUSDJPY moved to TARGET_SERIES
  - KXGAS reclassified to "energy" category

Local filter injection (bot/local_filter.py):
  - Jobless claims data injected for KXJOBLESS markets
  - EIA data injected for energy/gas markets
  - Fed data injected for KXFED/KXFOMC markets
  - Treasury data injected for yield/treasury markets
  - Congressional data injected for politics markets
  - Box office data injected for entertainment markets

Strategy updates:
  - 3 new strategies in dual_strategy.py: JOBLESS, ENERGY, CONGRESSIONAL
  - Rebalancer: 15% expansion pool across new categories
  - Morning review: data feed health check added

API keys needed for full functionality:
  - FRED_API_KEY (free at fred.stlouisfed.org) — powers jobless + fed + treasury
  - EIA_API_KEY (free at eia.gov/opendata) — powers petroleum data
  - Congressional + box office feeds work without API keys

================================================================
CURRENT STATE
================================================================

Alpaca: $99,975 (JNJ +$2.32, proper sizing)
Kalshi: 2 paper trades, 0 resolved
Regime: BEAR, Sentiment: Extreme Fear (11/100)
All 3 LLMs active, all safety guards deployed
Dashboard live, Telegram connected
Expansion: 6 new data feeds, 3 new strategies, 3 new categories

================================================================
PHASE 1: API COST OPTIMIZATION — 2026-03-26
================================================================

Universal API cache (bot/api_cache.py):
  - SQLite-backed cache for all cloud API calls
  - Per-model TTL: Perplexity 4hr, Claude 3hr, DeepSeek 3hr, Gemini 3hr
  - Cache key: hash of model + market title + date
  - Daily call budget tracking per model
  - Cache hit/miss stats logged

Perplexity priority gate:
  - Only fires for weather + tsa categories (live-changing data)
  - Requires local_conf > 0.70 AND price_gap > 0.10
  - Economic data markets skip Perplexity entirely (use Claude+DeepSeek)
  - PERPLEXITY_DAILY_BUDGET=50 hard cap in .env

Daily API budgets (.env):
  PERPLEXITY_DAILY_BUDGET=50
  CLAUDE_DAILY_BUDGET=75
  DEEPSEEK_DAILY_BUDGET=100
  GEMINI_DAILY_BUDGET=200

Gemini Flash integration (bot/ensemble_gemini.py):
  - google-generativeai SDK installed
  - Model: gemini-2.0-flash
  - Code functional, needs billing enabled at ai.google.dev

Estimated cost reduction:
  Before: ~$20-25/day (3,400 API calls, all 3 models on every market)
  After: ~$5-10/day (cache hits + Perplexity gate + budget caps)

================================================================
PHASE 2: TIERED ESCALATION — 2026-03-26
================================================================

Gemini verified: gemini-2.5-flash-lite working (2.0-flash deprecated for new users)

5-tier pipeline in ensemble_analyst.py:
  Tier 1 (local_only): conf>0.92 + prob extreme -> skip all cloud. $0/market.
  Tier 2 (gemini_only): Gemini agrees with local within 0.15 -> skip Claude/DS.
  Tier 3 (gemini_deepseek): Gemini+DS agree within 0.10 -> skip Claude.
  Tier 4 (full_ensemble): Claude tiebreaker when Gemini/DS disagree.
  Tier 5 (full_research): + Perplexity for live-data categories, borderline conf.

Each tier only fires if previous couldn't resolve. All calls cached.
execution_tier logged per market for cost tracking.

================================================================
PHASE 3: SMART CORRELATION GUARD — 2026-03-26
================================================================

Replaced hardcoded limits (mountain=2, weather/day=5) with bracket
mutual exclusivity detection in correlation_guard.py.

Weather: parses city+date+bracket from ticker, computes temp ranges,
  checks overlap. Non-overlapping brackets allowed simultaneously.
  Max 3 per city per resolution date (from .env).
  Combined probability cap at 0.90.
  Global weather cap: MAX_WEATHER_POSITIONS_PER_DAY=10 (from .env).

Economic: event-level dedup, max 3 brackets per event per release.

Backwards-compatible with executor.py call signature.

================================================================
PHASE 4: HOURLY DYNAMIC REBALANCER — 2026-03-26
================================================================

Rewrote strategy_rebalancer.py:
  - Rolling 48hr window from resolutions.sqlite (was 7-day from decisions)
  - 5% floor per active strategy
  - 70% distributed by win_rate * avg_edge
  - 25% volatility reserve (conf>0.85 + WR>65%)
  - Momentum: 3 wins -> +10%, 3 losses -> -50%
  - New categories (<10 resolved): fixed 8%
  - Hourly snapshots in rebalancer_history table
  - SP500 correctly penalized: 19% WR, 5-loss streak -> reduced allocation

================================================================
PHASE 5-6: MODEL UPGRADE + PRE-SCREENER — 2026-03-26
================================================================

llama3.2:3b: already present (pre-screener model)
qwen2.5:32b: downloading (~19GB)

Pre-screener (bot/pre_screener.py):
  - llama3.2:3b via Ollama, 10s timeout
  - Rejects markets where public data can't predict outcome
  - Wired into local_filter.py as first step before main model
  - Tested: passes weather/econ/inflation, rejects entertainment gossip
  - PRESCREENER_ENABLED=true in .env

================================================================
PHASE 7: LOCAL FILTER UPGRADED TO 32b — 2026-03-26
================================================================

qwen2.5:32b downloaded and active (19GB, was 4.7GB for 7b).
LOCAL_FILTER_MODEL=qwen2.5:32b in .env.
Test: NWS 58F vs 60F threshold -> prob=0.4, conf=0.7 (correct, no clustering).

================================================================
PHASE 8-9: MARKET CACHE + PARALLEL PROCESSING — 2026-03-26
================================================================

Market cache (bot/market_cache.py): thread-safe in-memory cache.
OLLAMA_KEEP_ALIVE=-1 set for persistent model loading.

Parallel processing: SKIPPED. Single-GPU Ollama queues internally.
ThreadPoolExecutor adds complexity without speedup. Pre-screener
(llama3.2:3b) handles the parallelism benefit by filtering before
the expensive 32b model runs.

================================================================
PHASE 10: CHROMADB VECTOR STORE — 2026-03-26
================================================================

ChromaDB 1.5.5 installed. bot/vector_store.py built.
all-MiniLM-L6-v2 embedding model downloaded.
48 resolved markets loaded. Similarity search working.
VECTOR_STORE_ENABLED=true in .env.
Persists to data/chromadb/ directory.

================================================================
PHASE 11: SPORTS PROPS — SKIPPED
================================================================

Sports markets are high-variance with limited public data edge.
No clean free data source found that provides actionable signal
for Kalshi sports prop markets. Skipped per instruction.

================================================================
PHASE 12-13: COST SECTION + POSITION LIMITS — 2026-03-26
================================================================

Morning review: API efficiency section added with per-model costs,
cache hits, and projected monthly spend.

Position limits raised and moved to .env:
  MAX_TRADE_SIZE=25 (was 10)
  MAX_NIGHTLY_SPEND=100 (was 50)
  MAX_WEATHER_POSITIONS_PER_DAY=10 (was 5)
  MAX_POSITIONS_PER_CITY_PER_DAY=3 (new)

All executor thresholds now read from .env with sensible defaults.

================================================================
PHASE 14: SELF-CALIBRATION ENGINE — 2026-03-26
================================================================

bot/self_calibrator.py — 4-tier reflection loop:

Tier 1 (hourly): Market movement reflector. Checks open positions
  for >20c adverse price movement. Flags for early exit review.
  Local 32b model only. No API cost. Tested: found 10 S&P flags.

Tier 2 (6-hour): Pattern analyzer. Groups losses by category/city/
  confidence bucket. Sends structured JSON to local 32b for pattern
  identification. Generates hypotheses, logs but does not apply.

Tier 3 (daily 2am): Adjustment executor. Reviews T2 hypotheses with
  supporting_trades > 5 and confidence medium/high. Applies to staging
  config with 24hr trial. Auto-reverts if WR doesn't improve by 3pts.

Tier 4 (weekly Sunday 3am): Benchmark comparison. Compares live WR
  per category against calibration/base_rates.txt. Telegram alert if
  drift exceeds 15pts. Tested: correctly flagged SP500 31% drift.

Safety: PROTECTED_PARAMS never modified, MAX_THRESHOLD_CHANGE=0.10,
all changes logged to calibration_history with full audit trail.

.env: SELF_CALIBRATION_ENABLED=true, SELF_CAL_MIN_TRADES=5,
  SELF_CAL_TRIAL_HOURS=24, SELF_CAL_MAX_THRESHOLD_CHANGE=0.10

================================================================
PHASE 15: SIMULATION BACKTESTING — 2026-03-27/28
================================================================

Simulation runner built to replay historical Kalshi markets through the
full pipeline (Ollama 32b scoring + executor gates) with actual historical
data injected as settlement source context.

calibration/simulation_runner.py — Weather simulation (1000 markets)
  Data source: Open-Meteo historical TMAX (free, no auth)
  Batch prefetches all city temps upfront (10 API calls total)
  Status: RUNNING at ~3.3 markets/min, ~95% win rate at 419/1000

calibration/simulation_categories.py — Multi-category simulator
  Waits for weather to complete, then runs sequentially:
  1. TSA (1255 markets) — data from local CSV data/raw/tsa/tsa_daily_volumes.csv
  2. CPI/Inflation (1371 markets) — data from FRED CPIAUCSL/CPIUFDSL/CPILFESL
  3. Jobless Claims (121 markets) — data from FRED ICSA
  4. Fed Rate (102 markets) — data from FRED DFEDTARU
  SKIPPED: EIA (0 markets in training data), Box Office (0 markets in training data)

Output databases (all separate from live):
  calibration/simulation_decisions.sqlite (weather)
  calibration/simulation_tsa.sqlite
  calibration/simulation_cpi.sqlite
  calibration/simulation_jobless.sqlite
  calibration/simulation_fed.sqlite

Weather interim results (419/1000):
  63 executed, 60W/3L, 95.2% WR
  All 3 losses under 3F NWS gap
  100% WR on 3-5F and 5-8F gaps (small sample)

IMPORTANT: Live bot is running independently. Simulations share Ollama
but do not touch decisions.sqlite, resolutions.sqlite, or any live DBs.

Simulation results (COMPLETE):
  Weather: 149 exec, 140W/9L, 94.0% WR, $1183 PnL
  TSA:     678 exec, 634W/44L, 93.5% WR, $6984 PnL
  CPI:     456 exec, 383W/73L, 84.0% WR, $3434 PnL
  Jobless:  67 exec, 31W/36L, 46.3% WR — NOT DEPLOYABLE
  Fed:      14 exec, 2W/12L, 14.3% WR — NOT DEPLOYABLE

City simulation running separately (calibration/simulation_city_level.py)

================================================================
PHASE 16: CITY-SPECIFIC WEATHER GATES — 2026-03-29
================================================================

Austin pattern confirmed across 3 live losses ($1.48 total loss).
NWS forecast accuracy for Austin specifically is lower due to
Hill Country geography and afternoon convection patterns.

bot/executor.py — Three new city-level gates:
  Gate 0.5: SUSPENDED_CITIES — blocks all trades for listed cities
    Austin suspended pending city simulation calibration
  Gate 0.7: CITY_MIN_CONFIDENCE — per-city confidence floor override
    Miami raised to 0.85 (coastal effects, sea breeze uncertainty)
  Gate 1.5: CITY_MIN_NWS_GAP — per-city NWS gap minimum
    Austin=6F (was 3F default)

.env config:
  SUSPENDED_CITIES=austin
  CITY_MIN_CONFIDENCE={"miami": 0.85}
  CITY_MIN_NWS_GAP={"austin": 6, "default": 3}

Debug logging: prints [city gate] SUSPENDED or CONFIDENCE when triggered.
Denver and Chicago are clean cities — system focuses weather capital there.

================================================================
PHASE 17: RESOLUTION-COUNT CALIBRATION TRIGGERS — 2026-03-29
================================================================

Replaced time-based self-calibration triggers with resolution-count cascade.
Primary triggers fire from resolution_tracker.py after each resolved trade.
Time-based fallback (72hr) kept in dual_strategy.py as safety net.

calibration_counters table in decisions.sqlite:
  tier, last_fired_at_count, current_count, threshold, last_fired_at

Tier thresholds:
  Tier 0:   every 1 resolution  — instant sanity check (high-conf losses)
  Tier 1.5: every 3             — market movement reflector
  Tier 2:   every 10            — pattern analyzer (category + city)
  Tier 2.5: every 25            — confidence recalibration
  Tier 3:   every 50            — adjustment executor (trial + auto-revert)
  Tier 3.5: every 200           — deep pattern review with LLM
  Tier 4:   every 500           — benchmark comparison
  Tier 5:   every 2000          — full system review + Telegram milestone

Self-calibrator Tier 2 city analysis:
  SELF_CAL_MIN_TRADES_CITY=3 (lower threshold for city-specific patterns)
  City-level prompt asks about geography, coastal effects, microclimate
  Category-level prompt now includes [city] tags on weather losses

Counters seeded at current resolution count (67) to prevent mass firing.

================================================================
PHASE 18: OPTIONS OBSERVER — 2026-03-29
================================================================

bot/options_observer.py — paper trading observation module:
  Pulls SPY/QQQ/sector ETF options chains on economic release signals
  Computes ATM straddle implied probability vs Kalshi probability
  Records options_edge_gap for correlation analysis
  Simulates paper positions when edge gap >= 5%
  Tracks positions every 30 min with Greeks snapshots
  Tables: observations, simulated_positions, outcomes, greeks_history
  OPTIONS_OBSERVER_ENABLED=true in .env

calibration/options_simulation.py — historical backtester:
  48 CPI release dates (2022-2025) + 30 Fed meeting dates
  Downloads historical SPY prices via yfinance
  Simulates straddle and directional paper trades
  Run: python calibration/options_simulation.py

Wired into dual_strategy.py (every 30 min alongside economics scanner)
Wired into self_calibrator Tier 2 (options pattern analysis every 10 obs)

================================================================
PHASE 19: DYNAMIC CAPITAL MANAGEMENT — 2026-03-29
================================================================

Replaced static MAX_NIGHTLY_SPEND with dynamic live balance system.

Capital tracking:
  Paper mode: $350 starting balance + resolved P&L = $302.64 current
  Live mode: calls Kalshi API via pykalshi for real balance
  capital_tracker table in decisions.sqlite logs every cycle

Floor protection:
  ACCOUNT_FLOOR_PCT=0.30 — floor = peak * 70%
  Available capital = balance - floor
  Current: $302.64 balance, $211.85 floor, $90.79 available
  Catastrophic halt at DRAWDOWN_HALT_PCT=0.25 + Telegram alert

Continuous performance score (10-100, starts 50):
  High-confidence win (>=0.85): +3 points
  Standard win: +2 points
  Any loss: -5 points
  Position multiplier = score/50 (1.0x at 50, 2.0x at 100)
  Daily deployment cap = (score/100) * 1.2 * available_capital
  Score persists across sessions, only changes on resolutions

Position sizing:
  Quarter-Kelly against effective available capital
  Effective capital = available * score_multiplier
  Per-trade cap: MAX_SINGLE_TRADE_PCT=0.20 (20% of effective)
  Category liquidity caps: weather=$150, TSA=$200, CPI=$400
  Minimum position: MIN_POSITION_SIZE=$15

.env config (replaces MAX_TRADE_SIZE / MAX_NIGHTLY_SPEND):
  PAPER_STARTING_BALANCE=350
  ACCOUNT_FLOOR_PCT=0.30
  DRAWDOWN_HALT_PCT=0.25
  MAX_SINGLE_TRADE_PCT=0.20
  MIN_POSITION_SIZE=15
  MAX_POSITION_SIZE_WEATHER=150
  MAX_POSITION_SIZE_TSA=200
  MAX_POSITION_SIZE_CPI=400

Morning review shows capital status, score trend, drawdown proximity.
Dashboard status.json includes capital_status for live monitoring.

================================================================
LIVE RESOLUTION SUMMARY — 67 resolved trades
================================================================

Overall: 15W/52L, 22.4% WR, -$47.36 PnL
  Forex: 49 trades, -$29.55 (pre-hard-block, disabled)
  Weather: 10 trades, 5W/5L, -$0.99
    Denver: 3W/0L (clean)
    Chicago: 1W/0L (clean)
    NYC: 1W/1L ($0.06 loss, pre-gate)
    Austin: 0W/3L, -$1.48 (SUSPENDED)
    Miami: 0W/1L, -$0.36 (conf raised to 0.85)

Deployable categories (from simulation):
  1. Weather — 94.0% WR (focus Denver/Chicago)
  2. TSA — 93.5% WR (set 10%+ gap minimum)
  3. CPI — 84.0% WR (set 0.5pp+ gap minimum)
  NOT deployable: Jobless (46.3%), Fed (14.3%)

================================================================
TO CHECK STATUS: ask Claude "status"
TO KILL: create S:\kalbot\STOP
TO DEPLOY DASHBOARD: cd S:\kalbot\dashboard && npx vercel --prod
